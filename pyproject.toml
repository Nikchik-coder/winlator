[project]
name = "s3-upload"
version = "0.1.0"
description = "S3 upload and management scripts"
requires-python = ">=3.8"
dependencies = [
    "boto3>=1.34.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "supabase>=2.6.0",
]
